# 华代优服 ERP - 软著代理签约客户资源池 V1

## 项目定位
软著代理专属客户资源池 ERP，核心是签约客户 + 软著案件 + 合同 + 财务 + 服务的一体化管理。

## 技术栈
- Next.js 最新版（App Router）
- TypeScript
- Tailwind CSS
- shadcn/ui（组件库）
- Zustand（状态管理）
- React Hook Form + Zod（表单校验）
- 所有数据用内存/本地 mock，不需要真实数据库

## 项目初始化
```
npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*" --use-npm
npx shadcn@latest init -d
npx shadcn@latest add button input label select table badge card dialog sheet tabs sidebar-provider sidebar
```

## 整体布局

### 侧边栏导航（固定左侧，可折叠）
图标 + 文字，分组：
- **概览** → 仪表盘
- **客户** → 客户列表、新建客户
- **案件** → 案件列表、新建案件
- **合同** → 合同列表
- **财务** → 收款管理、开票记录
- **服务** → 工单列表
- **报表** → 数据看板
- **系统** → 权限管理

顶部：Logo（华代优服 + 小字"软著代理管理系统"）+ 用户头像 + 下拉菜单

### 主内容区
面包屑导航 + 页面内容

---

## 页面实现清单

### 1. 仪表盘（`app/page.tsx` 或 `app/dashboard/page.tsx`）

**4个核心指标卡片（顶部）：**
- 本月新签客户数（数字 + 环比箭头）
- 在办案件数（数字 + 各状态小标签）
- 本月回款金额（金额 + 完成率进度条）
- 待处理工单数（数字 + 紧急标红）

**2列内容区：**
- 左：最近案件列表（表格：案件名、客户、状态badge、负责人、截止日期）
- 右：待办提醒列表（到期提醒、补正提醒、回款提醒，带颜色标记）

**底部：**
- 本月收入趋势（用简单的 div 高度模拟柱状图，不用图表库）

---

### 2. 客户列表（`app/customers/page.tsx`）

**顶部操作栏：**
- 搜索框（按客户名/联系人/信用代码）
- 筛选：签约状态下拉、服务等级下拉、负责人下拉
- 右：「新建客户」按钮

**数据表格（columns）：**
| 字段 | 说明 |
|------|------|
| 客户ID | 唯一ID，如 CU-2024-001 |
| 公司名称 | 可点击进详情 |
| 联系人 | 姓名 |
| 电话 | |
| 签约状态 | badge：签约中/已到期/已续签/流失 |
| 服务等级 | badge：VIP/重点/普通 |
| 在办案件数 | 数字 |
| 负责人 | |
| 签约日期 | |
| 操作 | 查看、编辑、新建案件 |

分页：每页20条，底部分页控件

**Mock数据：** 至少15条客户记录

---

### 3. 客户详情页（`app/customers/[id]/page.tsx`）

**顶部：** 公司名 + 签约状态badge + 服务等级 + 「编辑信息」按钮

**Tab切换（4个Tab）：**

**Tab1 - 基本信息：**
- 两列表格展示：公司名、统一社会信用代码、联系人、电话、邮箱、地址、行业、规模、软著需求类型、来源渠道、签约日期、到期日期、负责人、备注

**Tab2 - 软著案件：**
- 表格：案件名、软著类型、状态、提交日期、预计下证日期、费用、负责人
- 「新建案件」按钮

**Tab3 - 合同/财务：**
- 合同列表：合同号、服务内容、金额、付款状态、签约日期
- 回款记录：日期、金额、方式、备注

**Tab4 - 服务记录：**
- 工单列表：工单号、类型、内容摘要、状态、处理人、创建时间

---

### 4. 案件列表（`app/cases/page.tsx`）

**筛选：** 状态（全部/待提交/受理中/审查中/下证中/已完成）+ 类型 + 负责人 + 日期范围

**看板视图（默认）+ 表格视图（切换）：**

看板：按状态分5列，每个卡片显示：案件名、客户名、截止日期、负责人头像

表格：
| 案件ID | 软著名称 | 客户 | 类型 | 状态 | 提交日期 | 受理号 | 预计下证 | 费用 | 负责人 | 操作 |

**Mock数据：** 至少20条，覆盖所有状态

---

### 5. 案件详情页（`app/cases/[id]/page.tsx`）

**顶部：** 软著名称 + 状态badge + 客户名（可跳转） + 「编辑」按钮

**流程时间线（竖向）：**
材料收集 → 内部审核 → 提交国家版权局 → 受理 → 形式审查 → 实质审查 → 下证 → 交付 → 归档
每个节点：状态图标（完成/进行中/待处理）+ 时间 + 操作人 + 备注

**基本信息区：**
两列：软著名称、版本号、申请人、著作权人、开发完成日期、首次发表日期、软著类型、申请类型（普通/加急）、受理号、费用

**材料清单：**
Checklist形式：身份证明、营业执照、源代码截图、用户手册、说明文档 等，每项可标记已收/未收

**沟通记录（底部）：**
时间线形式，支持添加跟进记录

---

### 6. 收款管理（`app/finance/page.tsx`）

**顶部汇总卡片：**
- 本月应收 / 本月已收 / 未回款 / 逾期金额

**数据表格：**
| 客户 | 案件 | 应收金额 | 已收金额 | 待收金额 | 付款方式 | 到期日 | 状态 | 操作（登记收款） |

颜色标记：逾期红色、即将到期橙色、已完成绿色

---

### 7. 数据看板（`app/reports/page.tsx`）

**时间筛选：** 本周/本月/本季度/本年

**6个统计卡片：**
- 新签客户数 + 环比
- 在办案件数 + 分状态
- 本期收入 + 目标完成率
- 案件通过率
- 平均处理周期（天）
- 客户满意度（模拟评分）

**可视化（用 Tailwind 实现，不用图表库）：**
- 月度收入趋势：水平柱状图（div + 宽度百分比）
- 案件状态分布：进度条组合
- 负责人业绩排行：带进度条的排行榜

---

## Mock 数据要求
在 `lib/mock-data.ts` 统一定义：

**客户：** 15条，含不同状态、等级、行业的客户
**案件：** 20条，覆盖所有流程状态，关联到客户
**合同：** 10条，关联到客户和案件
**工单：** 10条，不同类型和状态
**收款记录：** 15条，含已收/未收/逾期

数据要真实感（软著行业相关公司名、软著名称等）

---

## 样式规范
- 主色：靛蓝 `#4F46E5`（按钮、激活态）
- 成功绿：`#10B981`
- 警告橙：`#F59E0B`
- 危险红：`#EF4444`
- 背景：`#F8FAFC`（浅灰）
- 卡片：白色背景 + `shadow-sm` + `border border-gray-100`
- 字体：系统默认

Badge 颜色：
- 已完成/已收款：绿色
- 进行中/部分收款：蓝色
- 待处理：灰色
- 逾期/紧急：红色
- 即将到期：橙色

---

## 验收标准
1. `npm run build` 无报错、无 TypeScript 错误
2. 侧边栏导航可正常跳转各页面
3. 客户列表有搜索和筛选功能（前端过滤 mock 数据）
4. 案件看板视图可正常展示
5. 客户详情 4 个 Tab 可切换
6. 案件详情流程时间线展示正确

## 完成后输出
「V1 完成」+ 已实现的页面列表 + 任何需要注意的问题
